const sentence = 'The quick brown fox jumps over the lazy dog.';
let index = 5;

console.log(`Using an index of ${index} the chracter returnd is ${sentence.at(index)}`)

index = -15;
console.log(`Using an index of ${index} the chracter returnd is ${sentence.at(index)}`)